content {
    # always use h2 as default headline
    defaultHeaderType = 2
}

styles.content {
  textmedia {
    # Max Image/Media Width
    maxW = 900

    linkWrap.lightboxEnabled = 1

    # No rel attribute needed for lightbox link
    linkWrap.lightboxRelAttribute =
  }
}
